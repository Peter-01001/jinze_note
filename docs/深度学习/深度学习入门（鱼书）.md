# 深度学习入门（鱼书）

## python

### python版本

+ 2.x 3.x 相互不兼容

### 数据类型

+ Python是属于“动态类型语言”的编程语言

+ **动态**：变量的类型是根据情况自动决定的

#### 列表（list）

+ **切片**:[]

    \>>>print(a)
    [1, 2, 3, 4, 99]
    \>>>a[0:2] # 获取索引为0到2（不包括2！）的元素
    [1, 2]
    \>>>a[1:] # 获取从索引为1的元素到最后一个元素
    [2, 3, 4, 99]
    \>>> a[:3] # 获取从第一个元素到索引为3（不包括3！）的元素
    [1, 2, 3]
    \>>> a[:-1] # 获取从第一个元素到最后一个元素的前一个元素之间的元素
    [1, 2, 3, 4]
    \>>> a[:-2] # 获取从第一个元素到最后一个元素的前二个元素之间的元素
    [1, 2, 3]

+ **字典**：{}

    \>>> me = {'height':180} # 生成字典 键值对
    \>>> me['height'] # 访问元素
    180
    \>>> me['weight'] = 70 # 添加新元素
    \>>> print(me)
    {'height': 180, 'weight': 70}

## 感知机

+ 神经元会计算传送过来的信号的总和，只有当这个总和超过
了某个界限值时，才会输出1

### 简单逻辑电路

#### 与门

#### 与非门

+ 与非门颠倒与门的输出

#### 或门

#### 导入权重和偏置

感知机构造：

$$
y = \begin{cases}
0 & (b + w_1 x_1 + w_2 x_2 \le 0) \\
1 & (b + w_1 x_1 + w_2 x_2 > 0)
\end{cases}
$$

    import numpy as np
    x = np.array([0, 1]) # 输入
    w = np.array([0.5, 0.5]) # 权重
    b = -0.7 # 偏置
    print(w*x)
    array([ 0. , 0.5])
    print(np.sum(w*x))
    0.5
    print(np.sum(w*x) + b)
    -0.19999999999999996 # 大约为-0.2（由浮点小数造成的运算误差）
